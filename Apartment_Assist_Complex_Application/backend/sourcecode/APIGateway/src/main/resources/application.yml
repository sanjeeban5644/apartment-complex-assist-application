server:
  port: 7000

spring:
  application:
    name: APIGateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: core-apartment-service
          uri: lb://COREAPARTMENTSERVICE
          predicates:
            - Path=/admin/**